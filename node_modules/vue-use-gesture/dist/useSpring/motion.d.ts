import { Ref } from 'vue-demi';
import { SpringConfig } from './presets';
declare type NumericalValues = Record<string, number> | number[];
export declare type SpringValue = NumericalValues;
export interface SpringObject<T extends SpringValue> {
    values: T;
}
export declare function useSpring<T extends SpringValue>(initialValue: T | Ref<T>, springConfiguration?: SpringConfig, options?: {
    onRest?: () => any;
}): T;
export {};
